<server>

    <featureManager>
        <feature>cdi-1.2</feature>
        <feature>jaxrs-2.0</feature>
        <feature>managedBeans-1.0</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080">
        <tcpOptions soReuseAddr="true"/>
    </httpEndpoint>

    <application name="MSurakkaAssignment" context-root="/MSurakkaAssignment" location="${appLocation}" type="war"/>

</server>
